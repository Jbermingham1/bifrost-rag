[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "bifrost-rag"
version = "0.1.0"
description = "Production RAG pipeline with vector retrieval and an evaluation harness — Precision@K, Recall@K, F1, MRR"
readme = "README.md"
license = "MIT"
requires-python = ">=3.11"
authors = [
    { name = "Jarrad Bermingham", email = "jarrad.bermingham@gmail.com" },
]
keywords = ["ai", "rag", "retrieval", "evaluation", "llm", "anthropic", "claude"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Topic :: Scientific/Engineering :: Artificial Intelligence",
    "Typing :: Typed",
]
dependencies = [
    "pydantic>=2.0",
    "numpy>=1.24",
]

[project.optional-dependencies]
anthropic = ["anthropic>=0.40.0"]
voyage = ["voyageai>=0.2.0"]
dev = [
    "pytest>=8.0",
    "pytest-cov>=5.0",
    "hypothesis>=6.0",
    "ruff>=0.4",
    "pyright>=1.1",
    "bandit>=1.7",
    "pip-audit>=2.7",
]

[project.scripts]
bifrost-rag = "bifrost_rag.cli:main"

[project.urls]
Homepage = "https://github.com/Jbermingham1/bifrost-rag"
Repository = "https://github.com/Jbermingham1/bifrost-rag"
Issues = "https://github.com/Jbermingham1/bifrost-rag/issues"

[tool.hatch.build.targets.wheel]
packages = ["src/bifrost_rag"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "--cov=bifrost_rag --cov-report=term-missing --cov-fail-under=80"

[tool.ruff]
target-version = "py311"
line-length = 100

[tool.ruff.lint]
select = ["E", "F", "W", "I", "N", "UP", "B", "A", "SIM"]
# TC rules suggest moving runtime-used imports into TYPE_CHECKING blocks when
# `from __future__ import annotations` is set. With that future, annotations are
# already free at runtime, so the move is pure boilerplate. Keeping imports flat
# improves readability for occasional contributors.

[tool.ruff.lint.per-file-ignores]
# Tests are allowed to assert on broad exceptions when validating input shape.
"tests/**/*.py" = ["B017"]

[tool.pyright]
pythonVersion = "3.11"
typeCheckingMode = "strict"
include = ["src"]
